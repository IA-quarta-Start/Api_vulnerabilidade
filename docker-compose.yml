version: '3.8'

services:
  db:
    image: postgres:latest
    environment:
      - POSTGRES_DB=startdb
      - POSTGRES_USER=final
      - POSTGRES_PASSWORD=final
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    ports:
      - "5436:5432"

volumes:
  postgres_data: